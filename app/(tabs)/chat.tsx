import React from 'react';
import { View, Text } from 'react-native';
import tw from 'tailwind-react-native-classnames';
import ChatSupport from '../(chat)';
// Avvalgi yozilgan kod

const ChatScreen = () => {
  return (
    <ChatSupport />
  );
};

export default ChatScreen;
